<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.isvirin.storeclient">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:name=".presentation.AndroidApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".presentation.view.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.view.activity.ProductDetailsActivity"
            android:label="@string/activity_title_product_details">
        </activity>

        <activity
            android:name=".presentation.view.activity.ProductListActivity"
            android:label="@string/activity_title_product_list">
        </activity>

        <activity
            android:name=".presentation.view.activity.CategoryListActivity"
            android:label="@string/activity_title_category_list">
        </activity>

        <activity
            android:name=".presentation.view.activity.BrandsByCategoryActivity"
            android:label="@string/activity_title_brand_list">
        </activity>

    </application>

</manifest>
